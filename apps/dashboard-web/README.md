# Dashboard Web

Minimal static frontend scaffold. Serves files from `public/` and talks to the API on the same host.

Run locally

```bash
cd apps/dashboard-web
npm install
npm start
```

Or run the full stack with `docker compose up --build` from the repo root.
